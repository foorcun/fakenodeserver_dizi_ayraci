import 'package:flutter/material.dart';

class AddDiziView extends StatelessWidget {
  const AddDiziView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Add dizi view"),
      ),
    );
  }
}
